<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="WLinkClone.Views.Home.Widgets.SpeedTestWidget"
    xmlns="http://xamarin.com/schemas/2014/forms" xmlns:material="clr-namespace:WLinkClone.Helpers"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <!--  Speed Test  -->
    <Frame>
        <StackLayout>
            <Label FontSize="16" Text="Speed Test" />
            <Label Text="Check your internet speed" />
            <Label
                FontFamily="{StaticResource Material}"
                FontSize="30"
                HorizontalOptions="End"
                Text="{x:Static material:MaterialIconHelper.Speedometer}"
                TextColor="{StaticResource Primary}" />
        </StackLayout>
    </Frame>
</ContentView>