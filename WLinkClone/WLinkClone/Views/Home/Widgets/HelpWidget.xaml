<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="WLinkClone.Views.Home.Widgets.HelpWidget"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="clr-namespace:WLinkClone.Helpers">
    <!--  Help  -->
    <Frame>
        <StackLayout>
            <Label FontSize="16" Text="Help" />
            <Label Text="Click Here to Help yourself with FAQs" />
            <Label
                FontFamily="{StaticResource Material}"
                FontSize="30"
                HorizontalOptions="End"
                Text="{x:Static material:MaterialIconHelper.HelpCircle}"
                TextColor="{StaticResource Primary}" />
        </StackLayout>
    </Frame>
</ContentView>