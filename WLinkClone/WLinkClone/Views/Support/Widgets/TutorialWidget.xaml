<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="WLinkClone.Views.Support.Widgets.TutorialWidget"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="clr-namespace:WLinkClone.Helpers">
    <Frame>
        <StackLayout>
            <Grid>
                <Ellipse
                    Fill="#e7eaff"
                    HeightRequest="60"
                    HorizontalOptions="Center"
                    WidthRequest="60" />
                <Label
                    FontFamily="{StaticResource Material}"
                    FontSize="40"
                    HorizontalOptions="Center"
                    Text="{x:Static material:MaterialIconHelper.PlayBox}"
                    TextColor="{StaticResource Primary}"
                    VerticalOptions="Center" />
            </Grid>
            <Label HorizontalOptions="Center" Text="Watch Tutorials" />
        </StackLayout>
    </Frame>
</ContentView>